npm init
--dependencia de produccion
npm i express
--dependencia de desarrollo si se necesita
npm i -D express

--Arrancar codigo
npm start

--Dependencia d edesarrollo
npm i -D nodemon

npm run server

npm i pug

npm i -D tailwindcss autoprefixer postcss postcss-cli

npx tailwindcss init -p

--complitar tailwindcss
npm run css

--instalar orm
npm i sequelizer mysql2

--para variables de entorno
npm i dotenv

---para validaciones de usuario
npm i express-validator


----Para Git
ssh-keygen -t rsa -b 4096 -C "davidscsebas@gmail.com"
eval "$(ssh-agent -s)"
ssh-add id_rsa
cat id_rsa.pub | clipcd

git branch <nombre-de-la-rama>
git push <nombre-remoto> <nombre-rama>
git checkout <nombre-de-la-rama>

git add .
git commit -m "mensaje de confirmación"
git push -u origin main


git pull
git remote add origin git@github.com:davidsilvaco/mypaw.git
git branch -M main
git commit -m 'Plantilla Base Mypaw'
 git push -u origin main

--crear rama dos alternativas
    - git branch experimental
    - git checkout -b otrarama
git show-branch
git checkout experimental
 git checkout dev
 --subir rama
git push -u origin experimental

git branch -d rama_a_borrar
git push origin --delete rama_a_borra_remoto

--El proceso para obtener una rama del repositorio remoto es bien sencillo. Lo conseguimos con el comando fetch.

git fetch

--Lanzado ese comando hemos podido descargar la rama git de remoto. Ahora podemos acceder a ella con los comandos que ya conoces.
git checkout mi_rama_remota_descargada